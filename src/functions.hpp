#ifndef FUNCTIONS_HPP
#define FUNCTIONS_HPP

#include <string>

std::string ExampleFunction(); // Declare it
int add(int a, int b);
bool is_even(int n);

// Add these declarations
void barFunction();
void bazFunction();

#endif
